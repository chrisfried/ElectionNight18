import { ContestSetPipe } from './contest-set.pipe';

describe('ContestSetPipe', () => {
  it('create an instance', () => {
    const pipe = new ContestSetPipe();
    expect(pipe).toBeTruthy();
  });
});
